module.exports = {
    project: {
        ios: {
            sourceDir: './ios',
        },
        android: {
            sourceDir: './android',
        }, // grouped into "project"
    },
    assets: ['./assets/fonts/'], // stays the same
};
